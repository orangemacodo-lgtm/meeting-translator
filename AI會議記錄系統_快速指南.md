# AI 會議記錄系統 - 快速指南

> 最後更新：2026-04-29  
> 適用版本：Week 3 醫療健康版  
> 本指南以 `C:\Users\orang\OneDrive\桌面\Claude-workspace\index.html` 目前行為為準。

## 先看這段

這套工具適合把會議、訪談、醫療討論或音檔整理成文字記錄，再輔助產生摘要與待辦事項。

使用前先確認四個邊界：

- **轉錄主要在瀏覽器本機執行**：Whisper 模型會在瀏覽器中運行，首次載入模型與套件需要網路，之後會依瀏覽器快取狀況重用。
- **不是嚴格全離線工具**：模型、前端套件、翻譯服務都可能需要網路；若要完全離線，需另行封裝模型與依賴。
- **自動翻譯會送出文字**：開啟「自動翻譯」時，轉錄文字會送到 LibreTranslate 服務；若內容含病歷或個資，請先關閉自動翻譯。
- **醫療摘要只能輔助整理**：摘要、決策、待辦與時間標記都需要人工複核，不可直接作為診療、病歷或法律依據。

---

## 30 秒快速開始

1. 用瀏覽器打開：`C:\Users\orang\OneDrive\桌面\Claude-workspace\index.html`
2. 建議先用 **Chrome 或 Edge 桌面版**。
3. 要現場錄音：選「Whisper 錄音」。
4. 要處理既有音檔：選「上傳音檔」。
5. 語言已知時，直接選「中文 / English / 日本語 / 한국어」，不要先用自動偵測。
6. 完成轉錄後，點「生成摘要」。
7. 複製 Markdown 版到 Notion、文件或病歷草稿；正式使用前務必人工校對。

---

## 我該選哪個模式？

### 模式 A：即時錄音

適合現場會議、看診討論、訪談。

操作步驟：

1. 選「Whisper 錄音」。
2. 選主要語言，中文場景建議選「中文」。
3. 音量正常先用預設增益；聲音太小再提高到 `2.5x` 到 `3x`。
4. 點紅色錄音按鈕開始。
5. 系統約每 15 秒切段轉錄一次。
6. 停止後檢查文字，再生成摘要。

注意：

- Medium 模型較精準但較慢、較吃記憶體；若卡住或失敗，改用 Small 或 Base。
- 「標示發言輪次」只是幫段落加輪次，不是辨識真實說話者姓名。
- Android 與 Safari 的穩定性可能較差；出問題時優先改用 Chrome/Edge 桌面版。

### 模式 B：上傳音檔

適合已錄好的 MP3、M4A、WAV、OGG、FLAC、WebM、MP4。

操作步驟：

1. 選「上傳音檔」。
2. 拖放或點選音檔。
3. 選音檔主要語言。
4. 保留「音訊增強」開啟。
5. 一般情況先不要開「調試模式」。
6. 點「開始轉錄」。

注意：

- M4A / AAC 會優先使用較保守的設定，通常會搭配 Small 模型與較高增益。
- 大檔案可能被自動改用較小模型以縮短時間。
- 若第一次結果太短、重複、空白，再改用調試模式。

### 模式 C：生成摘要

適合把已轉錄文字整理成結構化輸出。

操作步驟：

1. 確認轉錄文字已出現在文字框。
2. 點「生成摘要」。
3. 檢查 Markdown 與純文字版本。
4. 複製或儲存 `.md` / `.txt`。

---

## 推薦設定

| 設定 | 建議 | 原因 |
|------|------|------|
| 語言 | 已知語言就手動選 | 自動偵測可能誤判，尤其是短句與中英混合 |
| 音量增益 | 正常先 `1x` 到 `1.5x`；小聲用 `2.5x` 到 `3x` | 過高增益可能放大噪音 |
| 音訊增強 | 上傳音檔建議開啟 | 可改善低音量與部分噪音問題 |
| 顯示時間戳 | 建議開啟 | 方便回查原始錄音位置 |
| 每句換行 | 建議開啟 | 方便校對與整理 |
| 標示發言輪次 | 多人討論才開 | 只標段落輪次，不等於說話者辨識 |
| 自動翻譯 | 需要跨語言閱讀才開 | 開啟後會把文字送到外部翻譯服務 |
| 調試模式 | 只有難轉錄時才開 | 會增加耗時、模型載入與資源使用 |

---

## 模型怎麼選？

| 模型 | 大小 | 適合情境 | 風險 |
|------|------|----------|------|
| Tiny | 約 75MB | 快速測試、低效能裝置 | 品質較差，容易漏字 |
| Base | 約 140MB | 一般音檔、速度與品質平衡 | 專有名詞可能不穩 |
| Small | 約 240MB | 推薦作為穩定選項 | 比 Base 慢 |
| Medium | 約 770MB | 追求較高準確度 | 慢、吃記憶體，部分瀏覽器可能失敗 |

實用建議：

- 先求穩：Small。
- 機器較慢或檔案很長：Base。
- 內容很重要且電腦效能足夠：Medium。
- 只是快速試跑：Tiny。

---

## 調試模式

調試模式不是日常預設選項，它是「轉不出來時的排錯工具」。

建議開啟的情況：

- 轉錄結果只有幾個字。
- 轉錄結果大量重複。
- 音檔很小聲或噪音多。
- M4A / MP4 音檔一直失敗。
- Medium 模型載入或轉錄失敗。

調試模式會嘗試下列組合：

1. Tiny + `2.5x` + 音訊增強
2. Tiny + `3.0x` + 音訊增強
3. Base + `2.0x` + 音訊增強
4. Small + `1.5x` - 音訊增強
5. Small + `2.5x` - 音訊增強
6. Medium + `1.0x` + 音訊增強

重要限制：

- 調試模式主要在結果太差、空白或疑似幻覺時繼續下一組，不代表每次都完整比較 6 組。
- 調試摘要中的字數與品質標記只是參考，不能取代人工聽寫核對。
- 「最佳配置」應以實際內容正確率、完整度、噪音狀況一起判斷，不要只看字數。

---

## 醫療健康摘要

目前摘要是規則式整理，會從轉錄文字中抓出：

- 臨床重點 / 關鍵話題
- 診療決策 / 治療方案
- 待辦事項 / 後續追蹤
- 檢查 / 檢驗
- 用藥 / 治療
- 複查 / 追蹤
- 轉介 / 會診
- 時間標記 / 期限

可以手動標記重要事項：

```text
會議討論到高血壓控制狀況。
@李醫師開始調整降壓藥@
@一週後複查血壓@
```

系統會把 `@...@` 內容標示為手動項目。

醫療使用注意：

- 規則式摘要會漏抓，也可能誤抓。
- 關鍵詞不是診斷。
- 時間標記只抓文字表達，不理解完整醫囑脈絡。
- 用於醫療紀錄、轉介、衛教或院內分享前，必須由專業人員人工校對。
- 含病患姓名、病歷號、身分證號、電話、地址、影像報告等敏感資料時，請確認院內規範與授權。

---

## 隱私與連網說明

| 項目 | 目前行為 | 你需要注意 |
|------|----------|------------|
| Whisper 轉錄 | 音訊主要在瀏覽器中處理 | 首次載入模型與前端套件需要網路 |
| 模型下載 | 透過瀏覽器下載並快取 | 換瀏覽器、清快取或換裝置可能要重下 |
| 自動翻譯 | 轉錄文字會送到 LibreTranslate | 含個資或病歷時建議關閉 |
| 複製到剪貼簿 | 使用瀏覽器剪貼簿 API | 剪貼後請注意貼上的目的地 |
| 儲存文字 / 音檔 | 由瀏覽器下載到本機 | 檔案保存與分享仍需符合資料保護規範 |

如果目標是嚴格離線或院內合規部署，建議後續改成：

- 本機打包前端套件，避免 CDN。
- 預先下載並固定 Whisper 模型。
- 關閉外部翻譯，或改接院內翻譯服務。
- 增加個資遮罩與存取控管。

---

## 常見問題

**Q：為什麼轉不出來？**  
A：先確認麥克風權限、瀏覽器、語言設定與模型是否載入完成。上傳音檔可先用 Small / Base，仍失敗再開調試模式。

**Q：可以完全離線使用嗎？**  
A：目前不能保證。Whisper 轉錄在模型下載後可能可依快取離線使用，但首次載入、套件來源、模型快取與自動翻譯都會影響離線能力。

**Q：音檔會被上傳嗎？**  
A：依目前頁面設計，音訊轉錄主要在瀏覽器內處理；但自動翻譯會送出轉錄文字。若是敏感內容，請關閉自動翻譯並避免貼到外部服務。

**Q：中英混合可以嗎？**  
A：可以嘗試，但短句與混合語言可能誤判。建議選主要語言，轉錄後人工校對翻譯與專有名詞。

**Q：摘要準確嗎？**  
A：摘要是輔助整理，不是保證正確。音質、語速、口音、背景噪音、專有名詞與原始轉錄錯誤都會影響結果。

**Q：為什麼 Medium 反而出錯？**  
A：Medium 模型較大，對瀏覽器記憶體與裝置效能要求較高。失敗時改用 Small 或 Base 通常更穩。

**Q：翻譯沒出現怎麼辦？**  
A：LibreTranslate 可能暫時不可用，或網路連線被阻擋。可以先關閉自動翻譯，保留原文轉錄。

**Q：發言輪次能分辨誰在說話嗎？**  
A：不能。它只能協助分段標記，不是聲紋辨識，也不會自動知道講者姓名。

---

## 排錯清單

### 麥克風不能用

- 檢查瀏覽器是否允許麥克風權限。
- 改用 Chrome 或 Edge。
- 關閉其他正在占用麥克風的軟體。
- 重新整理頁面後再試。

### 模型下載或載入卡住

- 確認網路可連線。
- 改用較小模型，例如 Base 或 Small。
- 重新整理頁面。
- 清除瀏覽器快取後重試，但這會讓模型重新下載。

### 轉錄結果很短或空白

- 語言不要用自動偵測，改選主要語言。
- 提高音量增益到 `2.5x` 到 `3x`。
- 上傳音檔時開啟音訊增強。
- 改用 Small 或 Base。
- 最後再開調試模式。

### 轉錄出現大量重複文字

- 降低增益。
- 改用 Small 或 Base。
- 確認音檔不是長時間靜音。
- 用調試模式比較不同設定。

### 上傳音檔失敗

- 優先改成 WAV、MP3 或 M4A 再試。
- 檔案太大時先切短。
- Safari 失敗時改用 Chrome / Edge。
- 若 MP4 只含影片或音軌格式特殊，先抽出音訊。

### 摘要抓錯重點

- 先修正原始轉錄文字。
- 用 `@...@` 手動標記關鍵待辦。
- 對醫療內容逐項人工核對。
- 不要把摘要當成正式診斷或醫囑。

---

## 建議工作流程

### 一般醫療會議

```text
1. 用 Chrome / Edge 開啟 index.html
2. 選「Whisper 錄音」
3. 語言選「中文」
4. 開始錄音
5. 重要事項用 @...@ 手動標記
6. 停止錄音並檢查轉錄
7. 點「生成摘要」
8. 人工校對 Markdown 摘要
9. 去識別化後再分享或貼到正式系統
```

### 難轉錄音檔

```text
1. 上傳音檔
2. 語言手動選主要語言
3. 開啟音訊增強
4. 先用 Small 或 Base 轉一次
5. 若空白、太短或重複，再開調試模式
6. 比對調試結果與原音檔
7. 記錄本次最穩定設定供下次使用
```

---

## 已知限制

- 轉錄品質取決於音質，不是模型越大一定越好。
- 摘要邏輯以規則與關鍵詞為主，不具備完整醫療語意判斷。
- 自動翻譯依賴外部服務，可能失敗、延遲或翻錯專有名詞。
- 瀏覽器快取不是正式部署機制，清除快取後模型可能要重新下載。
- 本工具目前更適合個人輔助整理；若要院內正式使用，需要補上合規、權限、稽核、去識別化與資料保存策略。
